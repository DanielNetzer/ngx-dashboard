import { DragDelayLiftDirective } from './drag-delay-lift.directive';

describe('DragDelayLiftDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDelayLiftDirective();
    expect(directive).toBeTruthy();
  });
});
